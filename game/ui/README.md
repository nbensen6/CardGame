# /ui — anchor-based, scalable UI components

Flexible, anchor-based, scalable UI (CLAUDE.md §5). Support widely different
aspect ratios (tall/narrow phone vs wide monitor). Anchor and scale UI; never
hard-code pixel positions for one resolution. Size interactive targets for a
thumb.

Reusable UI building blocks live here; screen-specific composition lives in
`/views`.
