# root_lurker — refinement log

**Cannot score this pass. Full writeup in `design/progress/gale_serpent.md`
(2026-09-08 update) — this file records only what is specific to
`root_lurker`.**

Checked as one of the nine remaining original-cast beasts with no bare-name
progress file. `design/renders/root_lurker_pass1_34.png` and `_sil.png` show
a ring-of-roots fight arena, not the lurker itself. `design/progress/
root_lurker_ground.md` confirms these are the exact files it scored as the
ground, "captured with `look.sh env root_lurker 1`". Same `look.sh`/
`look.cmd` output-naming collision as `gale_serpent.md` describes.

Worth flagging for whoever fixes the naming and re-captures this one: this
is the beast backlog #90 says has an "adds" companion (the Root Tendril)
that never renders at all — the parent body's own render is unverifiable
too until this collision is fixed.

Needs a fresh `look.sh root_lurker 1` (cast) capture — ideally after the
naming collision itself is fixed — before this beast can be scored for real.
Not attempted here: a duty-1 diagnosis pass doesn't build renders, and fixing
shared tooling is duty-2-shaped, not this pass's job.
